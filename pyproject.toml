[project]
name = "mfa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "pyotp>=2.10.0",
    "qrcode[pil]>=8.2",
]
